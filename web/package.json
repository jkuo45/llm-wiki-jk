{
  "name": "llm-wiki-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "three": "^0.186.1"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "wrangler": "^4.0.0"
  }
}
